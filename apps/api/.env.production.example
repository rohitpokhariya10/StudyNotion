NODE_ENV=production
DEPLOYMENT_TIER=production
# For an HTTPS staging rehearsal, set DEPLOYMENT_TIER=staging and use matching
# rzp_test_ browser/server keys. Never use live Razorpay keys in public staging.
LOG_LEVEL=info
PORT=4000
FRONTEND_ORIGINS=https://app.example.com
APP_URL=https://app.example.com
PUBLIC_API_URL=https://api.example.com
BRAND_NAME=StudyNotion
BRAND_LOGO_URL=https://app.example.com/logo.png
SUPPORT_EMAIL=support@example.com
# Include an application database and durable write concern, for example:
# mongodb+srv://<user>:<password>@<cluster>/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://replace-with-secret-manager-value?retryWrites=true&w=majority
# The demo seed is always disabled for DEPLOYMENT_TIER=production.
REDIS_URL=rediss://default:replace-with-secret-manager-value@redis.example.net:6379
REDIS_CONNECT_TIMEOUT_MS=10000
REDIS_COMMAND_TIMEOUT_MS=5000
JWT_SECRET=replace-with-at-least-32-random-characters
OTP_SECRET=replace-with-a-different-32-character-secret
ALLOW_DEV_OTP=false

TRUST_PROXY=1
JSON_BODY_LIMIT=100kb
FORM_BODY_LIMIT=100kb
UPLOAD_MAX_BYTES=26214400
REQUEST_TIMEOUT_MS=120000
SHUTDOWN_TIMEOUT_MS=10000
MONGODB_MAX_POOL_SIZE=20
MONGODB_MIN_POOL_SIZE=1
MONGODB_SERVER_SELECTION_TIMEOUT_MS=10000
MONGODB_CONNECT_TIMEOUT_MS=10000
MONGODB_OPERATION_TIMEOUT_MS=15000
MONGODB_SOCKET_TIMEOUT_MS=30000
MONGODB_WAIT_QUEUE_TIMEOUT_MS=10000
MONGODB_AUTO_INDEX=false

COOKIE_NAME=studynotion_session
# Leave blank for a safer host-only cookie. Set only when audited subdomain
# sharing is an explicit deployment requirement; it must contain both public
# app and API hostnames.
COOKIE_DOMAIN=
COOKIE_SAME_SITE=lax
COOKIE_SECURE=true

# Required for production tier. Optional in staging; when omitted, also omit
# VITE_GOOGLE_CLIENT_ID so Google sign-in is hidden rather than broken.
GOOGLE_CLIENT_ID=replace-with-google-web-client-id.apps.googleusercontent.com
RESEND_API_KEY=re_replace_me
EMAIL_FROM=StudyNotion <noreply@example.com>
EMAIL_REPLY_TO=support@example.com
CONTACT_RECIPIENT=support@example.com

RAZORPAY_KEY_ID=rzp_live_replace_me
RAZORPAY_SECRET=replace-with-secret-manager-value
RAZORPAY_WEBHOOK_SECRET=replace-with-secret-manager-value

CLOUD_NAME=replace-me
CLOUD_API_KEY=replace-me
CLOUD_API_SECRET=replace-with-secret-manager-value
FOLDER_NAME=studynotion-production
MEDIA_URL_TTL_SECONDS=3600
CHECKOUT_TTL_SECONDS=1800
RAZORPAY_TIMEOUT_MS=10000
GOOGLE_TIMEOUT_MS=10000
REFUND_WINDOW_DAYS=7

# Set once to the exact UTC instant when Entitlement Stage 2 writers deploy.
# Eligibility requires Purchase.createdAt AND Purchase.paidAt at/after it.
# Drain or expire pre-boundary in-flight checkouts; they remain legacy-only.
# Runtime rejects a missing, malformed, or materially future boundary.
# Deployment configuration must pin the same reviewed value across restarts.
ENTITLEMENT_SIDECAR_STARTED_AT=
