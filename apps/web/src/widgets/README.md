# Widgets

Widgets compose reusable page regions from features, entities, and shared UI.
They do not issue network requests or own domain state.

Examples include the application shell, navbar, footer, dashboard sidebar,
curriculum panel, and review slider. Widgets may depend on features, entities,
and shared primitives, but not on pages or application composition.
